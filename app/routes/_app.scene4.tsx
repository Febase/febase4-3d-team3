import type { FC } from 'react';

export const handle = {
  offset: 64,
  dir: 'bottom',
};

const Scene4: FC = () => {
  return (
    <span
      style={{
        fontSize: 32,
        lineHeight: 1.5,
        fontWeight: 700,
        color: '#fff',
      }}
    >
      SCENE 4 PLACEHOLDER
    </span>
  );
};

export default Scene4;
