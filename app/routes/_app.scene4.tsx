import Scene4View from '~/components/Scene4View';

import type { FC } from 'react';

export const handle = {
  offset: 64,
  dir: 'bottom',
};

const Scene4: FC = () => {
  return <Scene4View />;
};

export default Scene4;
