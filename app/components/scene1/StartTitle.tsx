import * as styles from './StartTitle.css';

import type { FC } from 'react';

const StartTitle: FC = () => {
  return <span className={styles.title}>TITLE PLACEHOLDER</span>;
};

export default StartTitle;
