import { style } from '@vanilla-extract/css';

export const selector = style({
  height: '320px',
  display: 'flex',
  alignItems: 'center',
  flexWrap: 'wrap',
  width: '100%',
  justifyContent: 'space-around',
});
